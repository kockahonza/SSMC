# Package versions chosen to match what the authors most likely used:
# notebook kernel metadata says Python 3.6.2; repo data commits are Oct 2019 - Jan 2020
# (paper: Marsland, Cui & Mehta, Sci Rep 10:3308, Feb 2020).
numpy==1.17.4
scipy==1.3.3
pandas==0.25.3
matplotlib==3.1.2
seaborn==0.9.0
scikit-learn==0.21.3
statsmodels==0.10.2
scikit-bio==0.5.5
# Jupyter stack, contemporaneous versions that still support Python 3.6
ipython==7.9.0
ipykernel==5.1.3
notebook==6.0.2

# Build-time only (hdmedians, a scikit-bio dep, ships no pre-generated C):
#   cython==0.29.36  +  pip install --no-build-isolation scikit-bio==0.5.5
# community_simulator is installed from ./community-simulator, pinned to
#   commit 3c10480 (2020-01-23), i.e. the state of the package when the final
#   data was committed here (2020-01-21) and the paper appeared.
#   Installed with --no-deps: its setup.py requires cvxpy, which is only used by
#   Community.SteadyState() and is not used by this notebook.
